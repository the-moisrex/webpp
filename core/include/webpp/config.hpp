#ifndef CONFIG_H
#define CONFIG_H

namespace webpp {

    enum class config_format { Native, Ini };

    class config {
      public:
        config();
    };

} // namespace webpp

#endif // CONFIG_H
