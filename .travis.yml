dist: bionic
language: cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-snapshot
      - libboost-program-options-dev
      - libgtest-dev
      - cmake

before_install:
  - sudo mkdir /usr/src/googletest/googletest/build
  - cd /usr/src/googletest/googletest/build
  - sudo cmake ..
  - sudo make install
  - sudo rm -rf /usr/src/googletest/googletest/build
  - cd -

script:
  - mkdir build
  - cd build
  - CXX=/usr/bin/g++-10 CC=/usr/bin/gcc-10 cmake -DCMAKE_BUILD_TYPE=Debug ..
  - cmake --build .
  - ./webpptest
